{
  "name": "@lianyehdesign/design-tokens",
  "version": "0.1.0",
  "description": "Design tokens synced from Figma variables, built for iOS / Web / Android",
  "type": "module",
  "main": "dist/web/tokens.js",
  "types": "dist/web/tokens.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "ingest:api": "node scripts/ingest-figma-api.js",
    "normalize": "node scripts/normalize.js",
    "build": "node build.js",
    "all": "npm run normalize && npm run build"
  },
  "devDependencies": {
    "style-dictionary": "^4.3.0"
  },
  "license": "UNLICENSED",
  "private": true
}
